/**
 * Site configuration settings
 * 
 * This file contains global configuration settings for the website.
 * Edit these values to control different aspects of the site's behavior.
 */

const config = {
  /** 
   * Registration status
   * Set to true to allow registrations or false to close registrations
   */
  registrationOpen: false,
  
  /**
   * Registration closed message
   * This message will be displayed when registrations are closed
   */
  registrationClosedMessage: "Rejestracja na konferencję została zakończona. Dziękujemy za zainteresowanie!"
};

export default config;